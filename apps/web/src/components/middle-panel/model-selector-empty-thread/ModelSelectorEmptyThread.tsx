import type { ReactNode } from "react";

export function ModelSelectorEmptyThread({ children }: { children: ReactNode }) {
  return (
    <span className="contents" data-pencil-component="chpd8">
      {children}
    </span>
  );
}
