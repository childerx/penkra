import type { ReactNode } from "react";

export function EffortSelectorEmptyThread({ children }: { children: ReactNode }) {
  return (
    <span className="contents" data-pencil-component="N4buaG">
      {children}
    </span>
  );
}
