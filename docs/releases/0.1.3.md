# Penkra 0.1.3

- Restores the top-right control for opening the right-side panel.
- Allows the right-side panel to open before a panel has been added.
- Keeps diff access inside the panel's Add menu instead of duplicating it in the header.

The inherited Synara package version is upstream baseline metadata; Penkra desktop releases use
the version declared in `scripts/penkra-release.json`.
