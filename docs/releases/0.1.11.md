# Penkra 0.1.11

- Contains the complete consolidated-plan feature set: remote-authoritative HQ and client
  instructions, safe empty clears and workspace materialization, the restored top-right panel
  toggle, production client-schema isolation, and the authenticated private updater.
- Reuses the existing right dock without adding a dialog, sheet, sidebar, or custom viewer. The HQ
  icon opens `hq.md` and `client.md` as two ordinary dock tabs; a client icon opens only that
  client's remote record. Every tab renders one authorship through the existing `ChatMarkdown`.
- Restores the production update credential in the packaged app and pins backend `7290c38`, which
  includes canonical-policy enforcement for policy add/alter/drop and partition-safe isolation.
- Retains signed native-arm64 packaging, S3-compatible differential downloads, and the verified
  local update cache.
