# Penkra 0.1.8

- Contains the complete 0.1.7 feature set: live instruction viewers and editing, safe blank
  instruction materialization, production client-schema isolation guards, and the restored
  top-right panel toggle.
- Pins the backend at `0455f27`, including the PostgreSQL 16-compatible policy restoration guard.
- Bakes the production update credential into the signed desktop bundle so the newly installed app
  can authenticate its own startup and foreground update checks.
- Retains the S3-compatible differential updater and its verified local update cache.
