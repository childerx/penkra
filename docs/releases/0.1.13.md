# Penkra 0.1.13

- Delivers the complete reconciled platform implementation rather than publishing the obsolete
  `0.1.12` intermediate candidate.
- Keeps instruction notes in the existing right dock and ordinary Markdown file viewer. HQ opens
  the on-disk `hq.md` and `client.md` projections as files; no sheet, dialog, sliding sidebar,
  custom viewer, styling layer, or other invented notes UI is included.
- Refreshes open files and Explorer state through one app-wide native workspace watcher when
  external edits, reconciliation, or git changes occur.
- Bundles backend CLI commit `8084c89772ae1367bcb8f3bd8d07f355e7f8b723`, matching the deployed
  sandbox, channel, organization SDK, approval, instruction, and update-credential behavior.
- Retains the signed native-arm64 ZIP, authenticated production update feed, regenerated blockmap,
  and differential-download support. The unused Claude platform sidecar and DMG remain excluded.
