# Penkra 0.1.2

- Standardizes skill discovery in the `$` command lane.
- Adds client-specific instruction creation, editing, and workspace materialization.
- Improves instruction refresh reliability and release/update validation.

The inherited Synara package version is upstream baseline metadata; Penkra desktop releases use
the version declared in `scripts/penkra-release.json`.
