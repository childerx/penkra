# Penkra 0.1.5

- Preserves the verified macOS update archive after installation so the next release can reuse unchanged blocks.
- Completes the signed ZIP blockmap rollout and validates the real differential update path.
