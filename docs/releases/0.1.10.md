# Penkra 0.1.10

- Contains the complete consolidated-plan feature set: remote-authoritative HQ and client
  instructions, safe empty clears and workspace materialization, the restored top-right panel
  toggle, production client-schema isolation, and the authenticated private updater.
- Interim internal build that removed the standalone sheet and moved instructions into the existing
  right dock, but combined `hq.md` and `client.md` into one tab. Superseded by `0.1.11`, which keeps
  each remote authorship in its own ordinary dock tab.
- Pins the backend at `7290c38`, including canonical-policy enforcement for policy add/alter/drop,
  PostgreSQL 16-compatible restoration, and partition-safe client isolation.
- Retains signed native-arm64 packaging, S3-compatible differential downloads, and the verified
  local update cache.
