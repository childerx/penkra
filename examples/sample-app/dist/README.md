# Sample

This complete framework-neutral Penkra App demonstrates a page-owned App Bar, a route that omits
the bar, semantic Theme tokens, Space-partitioned settings, optional permission inspection, an
error state, a controller operation, and an awaited UI handoff.

Run `bun run build`, then `penkra app package dist --output sample.penkra`.
